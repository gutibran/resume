# resume
For the HR ladies.

```bash
# install latex
sudo apt install texlive-full

# clone repository
mkdir ~/Repositories
cd Repositories
git clone https://github.com/gutibran/resume

# build latex pdf
pdflatex ./resume.tex
```
